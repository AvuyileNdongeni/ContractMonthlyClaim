<Window x:Class="ContractMonthlyClaimSystemWPF.Views.Approvals"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Approvals" Height="400" Width="700">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Claims Awaiting Approval" FontSize="18" Margin="0,0,0,20"/>
            <DataGrid AutoGenerateColumns="False" Height="200">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ClaimID"/>
                    <DataGridTextColumn Header="Lecturer"/>
                    <DataGridTextColumn Header="Month/Year"/>
                    <DataGridTextColumn Header="Amount"/>
                    <DataGridTextColumn Header="Status"/>
                    <DataGridTemplateColumn Header="Actions">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Verify" Width="60" Margin="2"/>
                                    <Button Content="Approve" Width="60" Margin="2"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</Window>
